export class Category {
  id : number;
  name: string;
  imgCate : string;

  constructor(id: number, name: string, imgCate: string) {
    this.id = id;
    this.name = name;
    this.imgCate = imgCate;
  }
}

